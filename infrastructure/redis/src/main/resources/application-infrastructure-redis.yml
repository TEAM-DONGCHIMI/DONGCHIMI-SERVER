spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

view-count:
  dedup-ttl: ${VIEW_COUNT_DEDUP_TTL:PT24H}

account-cache:
  ttl: ${ACCOUNT_CACHE_TTL:PT10M}

holiday-cache:
  ttl: ${HOLIDAY_CACHE_TTL:P1D}
  fallback-ttl: ${HOLIDAY_CACHE_FALLBACK_TTL:PT10M}
