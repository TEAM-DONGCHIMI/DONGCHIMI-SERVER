{
	email {$ACME_EMAIL}
	# 최초/테스트 발급 검증 시에만 활성화 (Let's Encrypt rate limit 회피). 검증 후 제거하고 재기동:
	# acme_ca https://acme-staging-v02.api.letsencrypt.org/directory
}

{$DOMAIN} {
	# 활성 색(app-blue 또는 app-green)을 가리키는 한 줄만 분리한다.
	# 배포 스크립트가 upstream.conf를 갈아끼운 뒤 `caddy reload`로 무중단 전환한다.
	import /etc/caddy/upstream.conf
}